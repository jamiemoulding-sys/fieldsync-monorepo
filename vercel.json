{
  "installCommand": "npm install",
  "buildCommand": "npm run build:shared && npm --workspace Frontend/fieldsync-frontend run build",
  "outputDirectory": "Frontend/fieldsync-frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
